<p id="notice"><%= notice %></p>
<div style="height:100%; width:100%; border:3px solid black;">
  <%= gravatar_for @user %>
  <%= @user.name %>
</div>

<div class="stats">
  <a href="<%= worked_at_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.companies.count %>
    </strong>
    Companies connected with
  </a>
</div>
 <% @user.companies.each do |company| %>
                        <div class="profile">
                            <!--<%= link_to (image_tag("http://www.gravatar.com/avatar.php?gravatar_id=#{company.name}", :alt => 'Avatar', :class => 'img-circle')),user_path(company) %> -->

                            <h3 class="name">
                                <%= company.name %>
                            </h3>
    
                            <!-- DO NOT HAVE LOCATION ENTITY -->
                        </div>
                        <% end %>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
