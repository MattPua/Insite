<%= form_for(current_user.relationships.build(interview_id: @interview.id),  remote: true) do |f| %>
  <div><%= f.hidden_field :interview_id %></div>
  <%= f.submit "Schedule an Interview!", class: "btn btn-large btn-primary" %>
<% end %>