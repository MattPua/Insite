<%= form_for(current_user.relationships.find_by(company_id: @company),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Fired!", class: "btn btn-large" %>
<% end %>