<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="form-content" class="row">
		<!-- add span 1 as placeholder -->
		<div class = "col-md-3"></div>
		
		<div class = "register-form col-md-6">
				<div class="form-group">
					<%= f.text_field :name, class:"form-control input-lg", placeholder:"Full Name"%>
				</div>
				<div class="form-group">
					<%= f.email_field :email, :autofocus => true, class:"form-control input-lg", placeholder:"Your UTOR email address" %>
				</div>
				<div class="form-group">
					<%= f.number_field :year, class:"form-control input-lg", placeholder:"Graduating Year"%>
				</div>
				<div class="form-group">
					<%= f.number_field :year, class:"form-control input-lg", placeholder:"Graduating Year"%>
				</div>
				<div class="form-group">
					<%= f.text_field :faculty, class:"form-control input-lg", placeholder:"Program"%>
				</div>
				<div class="form-group">
					<%= f.number_field :phone , class:"form-control input-lg", placeholder:"Phone Number (Optional)"%>
				</div>
				<form>
				<div class=" form-group">
				<button type="button" class="btn btn-default">Default</button>
					<%= f.cancel "Cancel", href: "<%= link_to 'Back', users_path %>" ,class: "btn  btn-lg btm-warning pull-right" %>
					<%= f.submit "Submit", class:"btn btn-lg btn-warning pull-right"%>
				</div>
				<!-- <div><%= f.submit "Submit" %></div> -->
		</form>
			</div>
<% end %>
