<style type="text/css">
    #header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 80px;
    padding: 0;
    margin: 0;
    margin-bottom: 80px;
    z-index:99;
    }

    #header #header-background {
    background: #5d86b2;
    filter:alpha(opacity=20); /* IE */
    -moz-opacity:0.2; /* Mozilla */
    opacity: 0.2; /* CSS3 */
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width:100%;
    }

    #logo a:hover{
    background-color: transparent;
    }

</style>
<div id="header">
    <!-- for transparent header background -->
    <% if (params[:controller]=="home" && params[:action]=="index")%>
    <div id="header-background" ></div>
    <% end %> 
        <div style="position: absolute; width: 100%; height: 80px; margin:0;
        <%= "background: #399996;" unless (params[:controller]=="home" && params[:action]=="index")%>">



			<div id="logo" class="pull-left" style="margin-top: -7px; margin-left: 10px; height: 100%">
				<%= image_tag("insite-logo.png") %>
			</div>
        <!--  <% if user_signed_in? %>
          <span style=" text-align:center; ">

                <%= form_tag users_path, style:"margin:3% auto; width:20%;", method: :get do %>
                <%= text_field_tag :search, ''    %>
                <%= submit_tag "Search", :name => nil %>
                  <% end %>
          <span>
          <% end %>-->
			<div id="nav" class="pull-right" style="margin-top:1%; margin-right: 30px; height: 100%">


            <% if !user_signed_in? %>
				<div id="nav" class="pull-right" style="margin-top: 8px; margin-right: 30px; height: 100%">
                <%= link_to "Log In", new_user_session_path, class:"btn btn-lg btn-default"%>
                <%= link_to "Sign Up", new_user_registration_path , class:"btn btn-lg btn-warning"%>
            </div>
            <% else %>
          
          
            <div id="nav" class="pull-right" style="margin-top: 8px; margin-right: 30px; height: 100%">
                <%= link_to "#{current_user.name}", user_path(current_user), class:"btn btn-lg btn-default" %>
                <%= link_to "Log Out", destroy_user_session_path, method: :delete, class:"btn btn-lg btn-warning" %>
            </div>
            <% end %>
        </div>
    </div>
</div>

<!-- dummy header for non transparent banner -->
<div style="
    <%= "height: 80px;" unless (params[:controller]=="home" && params[:action] =="index")%>">
</div>

