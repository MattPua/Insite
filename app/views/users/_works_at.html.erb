<%= form_for(current_user.relationships.build(company_id: @company),  remote: true) do |f| %>
  <div><%= f.hidden_field :company_id %></div>
  <%= f.submit "Work Here Now!", class: "btn btn-large btn-primary" %>
<% end %>