<div id="flash_messages" style="text-align:center; width:auto;">
	<% flash.each do |name, msg| %>
	  <% if msg.is_a?(String) %>
	    <%= content_tag :div, msg, :id => "flash_#{name}" %>
	  <% end %>
	<% end %>
</div>