<%= form_for(current_user.relationships.find_by_company_id(@company.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Fired!", class: "btn btn-large" %>
<% end %>